package com.bayviewglen.dayone;

public class StringClass {

	public static void main(String[] args) {
	
		String word = "Alphabet";
		String num = "723";
		System.out.println(num + 6);
		
		////////////////////////////
		
		char letter = 'E';
		
		// Method: Length()
		// Activate/call the method using the object/variable ie. word.length
		// Last index is always length()-1
		
		// Method: indexOf('p')... It will return 2, because it found the "p" at 2. 
		// indexOf() looks for where the word starts, the first index of that word
		// If the word that your looking for is not there, it will return -1
		
		// word.substring(1, 4); This means give me the index of 1 - 3... 1 is inclusive, 4 is exclusive
		// word.substring(3); "Alphabet"... This will return "habet"
		
		// word.charAt(4) = word.character at index 4 = 'a'
		// word.charAt(word.length)/2) = 8/2 = 4
		
		// Scanner keyboard = newScanner(System.in);
		// Keyboarf.nextLine(); = reads a line up to the enter key
		
		
		
	}

}
